import { Text, View } from "react-native";

export default function HomePage() {
  return (
    <View className={"w-full h-screen flex items-center justify-center"}>
      <Text className={"text-4xl font-bold"}>Hello Expo!</Text>
    </View>
  );
}
